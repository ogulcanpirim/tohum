import React from 'react';
import styles from '../screens/styles';
import { Text } from 'react-native';

const LogoComponent = () => {

    return (
        <Text style = {styles.logoTitle}>tohum</Text>
    );
}

export default LogoComponent;